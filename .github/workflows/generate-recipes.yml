name: Generate Recipe Pages

on:
  push:
    paths:
      - "recipes/*.json"
      - "templates/recipe-template.html"
      - "tools/generate_recipe.py"
      - "tools/run_bear_os.py"
      - "tools/generate_social_content.py"
      - "tools/generate_pinterest.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run Bear OS Generator Engine
        run: python tools/run_bear_os.py

      - name: Commit generated pages
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Bear OS generated content
          file_pattern: |
            recipes/*.html
            outputs/**
